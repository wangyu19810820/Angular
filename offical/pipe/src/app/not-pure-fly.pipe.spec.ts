import { NotPureFlyPipe } from './not-pure-fly.pipe';

describe('NotPureFlyPipe', () => {
  it('create an instance', () => {
    const pipe = new NotPureFlyPipe();
    expect(pipe).toBeTruthy();
  });
});
